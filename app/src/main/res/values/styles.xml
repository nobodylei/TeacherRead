<resources>

    <!-- Base application theme. -->
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#3FEBBB</item>
        <item name="colorPrimaryDark">#3FEBBB</item>
        <item name="colorAccent">@android:color/darker_gray</item>
    </style>

    <style name="spinner_style">
        <item name="android:background">@drawable/withdraw_spinner</item>
        <item name="android:popupBackground">@drawable/withdraw_spinner</item>
    </style>

    <style name="wel" parent="@android:style/Theme.NoTitleBar.Fullscreen">
        <item name="android:windowBackground">@drawable/wel2</item>
        <item name="android:windowEnterAnimation">@animator/context_menu_enter</item>
        <item name="android:windowExitAnimation">@animator/context_menu_exit</item>
    </style>

    <style name="dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item><!--边框-->
        <item name="android:windowNoTitle">true</item><!--无标题-->
        <item name="android:windowIsFloating">true</item><!--是否浮现在activity之上-->
        <item name="android:windowIsTranslucent">false</item><!--半透明-->
        <item name="android:windowBackground">@color/white</item><!--背景透明-->
    </style>
</resources>
